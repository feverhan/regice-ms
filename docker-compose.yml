services:
  regice-ms:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: regice-ms
    restart: unless-stopped
    ports:
      - "${APP_PORT:-5000}:5000"
    environment:
      PORT: 5000
    volumes:
      - ./fridge_inventory.json:/app/fridge_inventory.json
